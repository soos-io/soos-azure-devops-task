{
  "name": "soos-azure-devops-task",
  "description": "SOOS Azure DevOps Security Analysis Task. Run SOOS SCA, DAST, SAST, SBOM, and Container analysis from your Azure DevOps pipeline. Register for a free trial at https://app.soos.io/register",
  "author": "SOOS",
  "publisher": "SOOS",
  "license": "MIT",
  "_comment": "NOTE: Version is set by the build pipeline",
  "version": "1.0.0",
  "main": "build/task.js",
  "scripts": {
    "setup:install": "npm install",
    "setup:clean-install": "npm ci",
    "setup:update": "npx npm-check -u",
    "setup:clean": "npx rimraf node_modules && npx rimraf package-lock.json",
    "build": "tsc",
    "build:clean": "npx rimraf build",
    "format": "prettier ./src --check",
    "format:fix": "prettier ./src --write",
    "lint": "eslint --max-warnings 0",
    "lint:fix": "eslint --max-warnings 0 --fix",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && mocha build/test/_suite.js",
    "test:coverage": "npm run test -- --reporter xunit --reporter-option output=ResultsFile.xml",
    "check": "npm run build:clean && npm run format && npm run lint && npm run typecheck && npm run test && npm outdated",
    "link:soos-api-client": "npm link @soos-io/api-client && npm run build && npm link",
    "patch-api-client": "npm run clean:all && npm install @soos-io/api-client@latest --save-exact && npm install && npm run check",
    "clean": "npm run build:clean",
    "clean:all": "npm run clean && npm run setup:clean",
    "knip": "knip"
  },
  "dependencies": {
    "@soos-io/api-client": "1.3.0",
    "azure-pipelines-task-lib": "^5.0.0",
    "azure-pipelines-tasks-docker-common": "^2.254.0",
    "core-js": "^3.41.0",
    "tslib": "^2.8.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.17.14",
    "eslint": "^9.21.0",
    "globals": "^16.0.0",
    "knip": "^5.46.0",
    "mocha": "^11.0.1",
    "prettier": "^3.3.3",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.1"
  }
}
